<!-- <h1>Attractions</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>

      <td><strong>Name</strong></td>
      <td><strong>Tickets</strong></td>
      <td><strong>Details</strong></td>
    </tr>

    <% @attractions.each do |attraction| %>
      <tr>
        <td><%= attraction.name %>
        <td><%= attraction.tickets %>
        <td>Ride the <%= link_to attraction.name, attraction %>
      </tr>
      <% end %>

  </tbody>
</table> -->
<%if flash[:msg] %>
  <%= flash[:msg] %>
<% end %>

<%= render "standard_index" %>
<% if @user.admin %>
  <%=link_to "Create a new attraction!", new_attraction_path %>
<% end %>
